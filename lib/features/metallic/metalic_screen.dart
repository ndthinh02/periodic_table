import 'package:flutter/material.dart';

class MetalicScreen extends StatelessWidget {
  const MetalicScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('BlockScreen'),
      ),
    );
  }
}
